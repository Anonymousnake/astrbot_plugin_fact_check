{
  "enable_fact_check": {
    "type": "bool",
    "description": "Enable /事实核查 command",
    "hint": "Supports quoted text, inline claims, and quoted/inline images on OneBot/AstrBot message chains.",
    "default": true
  },
  "gemini_api_key": {
    "type": "string",
    "description": "Gemini API key for fact-checking",
    "hint": "Leave empty to use GEMINI_API_KEY environment variable.",
    "default": ""
  },
  "gemini_base_url": {
    "type": "string",
    "description": "Gemini API base URL",
    "default": "https://generativelanguage.googleapis.com/v1beta/models"
  },
  "fact_check_pre_model": {
    "type": "string",
    "description": "Pre-processing model for extracting checkable claims",
    "default": "gemini-3.1-flash-lite"
  },
  "fact_check_main_models": {
    "type": "list",
    "description": "Main fact-check model fallback order",
    "hint": "The first model is tried first. Later models are used on rate-limit/server errors.",
    "default": [
      "gemini-3-flash-preview",
      "gemini-2.5-flash",
      "gemini-3.1-flash-lite"
    ]
  },
  "fact_check_max_images": {
    "type": "int",
    "description": "Max images to inspect per fact-check request",
    "default": 2
  },
  "fact_check_max_image_bytes": {
    "type": "int",
    "description": "Max bytes per image before it is split into compressed chunks",
    "default": 5242880
  },
  "fact_check_long_image_chunk_height": {
    "type": "int",
    "description": "Chunk height used when an oversized long image is split",
    "default": 2200
  },
  "fact_check_long_image_max_parts": {
    "type": "int",
    "description": "Max image chunks generated from one oversized long image",
    "default": 8
  },
  "fact_check_long_image_max_width": {
    "type": "int",
    "description": "Max width for oversized long-image chunks",
    "default": 1280
  },
  "fact_check_total_timeout_seconds": {
    "type": "int",
    "description": "Total timeout seconds for one fact-check request",
    "hint": "Prevents image extraction or Gemini grounding from making the QQ command look unresponsive.",
    "default": 180
  },
  "fact_check_max_concurrent": {
    "type": "int",
    "description": "Max concurrent fact-check API jobs",
    "hint": "Keep this low to avoid Gemini rate-limit bursts.",
    "default": 1
  },
  "fact_check_max_queue": {
    "type": "int",
    "description": "Max queued/running fact-check jobs before rejecting new requests",
    "default": 4
  },
  "fact_check_cache_ttl_seconds": {
    "type": "int",
    "description": "Seconds to reuse the same fact-check reply for identical input",
    "default": 600
  },
  "fact_check_cache_max_entries": {
    "type": "int",
    "description": "Max in-memory fact-check cache entries",
    "default": 32
  },
  "fact_check_followup_ttl_seconds": {
    "type": "int",
    "description": "Seconds to keep a fact-check result available for reply follow-ups",
    "default": 3600
  },
  "fact_check_followup_max_sessions": {
    "type": "int",
    "description": "Max in-memory fact-check follow-up sessions",
    "default": 50
  },
  "fact_check_rate_limit_cooldown_seconds": {
    "type": "int",
    "description": "Cooldown after Gemini reports 429/rate-limit",
    "default": 90
  },
  "fact_check_image_download_timeout_seconds": {
    "type": "int",
    "description": "Timeout seconds for downloading each QQ image",
    "default": 10
  },
  "fact_check_forward_max_fetch": {
    "type": "int",
    "description": "Max merged-forward payloads to expand from a quoted/direct QQ chat-record card",
    "hint": "Includes nested forward ids. Keep small so one deep chat record cannot stall the command.",
    "default": 3
  },
  "fact_check_pre_timeout_seconds": {
    "type": "int",
    "description": "Timeout seconds for claim extraction requests",
    "default": 35
  },
  "fact_check_main_timeout_seconds": {
    "type": "int",
    "description": "Timeout seconds for the main grounded fact-check request",
    "default": 70
  },
  "fact_check_anysearch_enabled": {
    "type": "bool",
    "description": "Enable Anysearch pre-retrieval evidence before the final Gemini fact-check",
    "hint": "When enabled, extracted claims are sent to Anysearch for search snippets and optional public-page extraction. This supplements but does not replace Gemini Google Search grounding.",
    "default": false
  },
  "fact_check_anysearch_endpoint": {
    "type": "string",
    "description": "Anysearch MCP JSON-RPC endpoint",
    "default": "https://api.anysearch.com/mcp"
  },
  "fact_check_anysearch_api_key": {
    "type": "string",
    "description": "Anysearch API key",
    "hint": "Optional. Leave empty to use anonymous Anysearch access or ANYSEARCH_API_KEY environment variable.",
    "default": ""
  },
  "fact_check_anysearch_timeout_seconds": {
    "type": "int",
    "description": "Timeout seconds for each Anysearch search/extract call",
    "default": 20
  },
  "fact_check_anysearch_max_claims": {
    "type": "int",
    "description": "Max extracted claims to search with Anysearch",
    "default": 3
  },
  "fact_check_anysearch_max_results_per_claim": {
    "type": "int",
    "description": "Max Anysearch search results per claim",
    "default": 3
  },
  "fact_check_anysearch_extract_top_urls": {
    "type": "int",
    "description": "How many top public result URLs to extract into Markdown evidence",
    "default": 2
  },
  "fact_check_anysearch_max_chars": {
    "type": "int",
    "description": "Max characters of Anysearch evidence inserted into the final fact-check prompt",
    "default": 6000
  },
  "fact_check_anysearch_freshness": {
    "type": "string",
    "description": "Optional Anysearch freshness filter: day, week, month, or year",
    "default": ""
  },
  "fact_check_anysearch_content_types": {
    "type": "list",
    "description": "Anysearch content type filters",
    "hint": "Supported values include web, news, doc, academic, and data.",
    "default": [
      "web",
      "news"
    ]
  },
  "fact_check_show_failure_reason": {
    "type": "bool",
    "description": "Reply with a short reason when fact-checking fails",
    "default": true
  }
}
